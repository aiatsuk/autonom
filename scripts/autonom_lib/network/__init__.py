"""Network capture and mocking (Phase 3).

`mitm_addon.py` is deliberately not imported here: it runs inside mitmproxy's own
interpreter and must stay importable without this package on the path.
"""
