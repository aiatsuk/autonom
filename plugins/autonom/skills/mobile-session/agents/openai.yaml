interface:
  display_name: "Mobile Session"
  short_description: "Explicit Android and iOS Simulator sessions for agent test/debug"
  default_prompt: "Start an Autonom device session on one explicit target, install or launch the app if needed, and keep artifacts and the journal under ~/.autonom/sessions/."
