# Autonom plugin payload

This directory is the portable skill pack referenced by
`../../.agents/plugins/marketplace.json` (Codex) and by
`scripts/install_skills.sh` for Claude, Grok, and local installs.

See the repository-level README for multi-agent installation and development
instructions.
